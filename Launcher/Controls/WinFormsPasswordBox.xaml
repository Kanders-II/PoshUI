<UserControl x:Class="Launcher.Controls.WinFormsPasswordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
             xmlns:local="clr-namespace:Launcher.Controls"
             x:Name="root">
    <UserControl.Resources>
        <!-- Small neutral ToggleButton style for Reveal control -->
        <Style x:Key="RevealToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource BodyForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,2"/>
            <Setter Property="MinWidth" Value="48"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="BtnBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="{DynamicResource IconButtonHoverBrush}"/>
                            </Trigger>
                            <!-- Keep background transparent when checked to maintain text contrast in dark theme -->
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="BtnBorder" Property="Background" Value="Transparent"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <!-- Host for WinForms MaskedTextBox wrapped in a themed Border to match TextBox styling -->
        <Border Grid.Column="0"
                Background="{DynamicResource TextBoxBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="0"
                SnapsToDevicePixels="True">
            <wfi:WindowsFormsHost x:Name="Host" Background="Transparent" Margin="4,2,4,2"/>
        </Border>
        <!-- Toggle button to reveal/mask -->
        <ToggleButton x:Name="RevealToggleButton" Grid.Column="1"
                      Width="Auto" Margin="6,0,0,0"
                      Style="{StaticResource RevealToggleButtonStyle}"
                      Checked="RevealToggleButton_Checked" Unchecked="RevealToggleButton_Unchecked"
                      ToolTip="Show/Hide password">
            <TextBlock x:Name="RevealLabel" Text="Show" FontSize="12" FontWeight="SemiBold"/>
        </ToggleButton>
    </Grid>
</UserControl>
 